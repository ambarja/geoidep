# Time format units This code transforms the time from milliseconds to a calendar date format.

Time format units This code transforms the time from milliseconds to a
calendar date format.

## Usage

``` r
as_data_time(x)
```
