% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_fill_mapbiomas_peru_fire_d.R
\name{scale_fill_mapbiomas_peru_fire_d}
\alias{scale_fill_mapbiomas_peru_fire_d}
\title{Discrete fill scale for MapBiomas Peru Fire products}
\usage{
scale_fill_mapbiomas_peru_fire_d(
  product,
  ...,
  lang = c("en", "es"),
  na.translate = FALSE
)
}
\arguments{
\item{product}{Character. One of the products listed in
\code{\link{get_mapbiomas_peru_fire_products}}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2]{scale_fill_manual}}.}

\item{lang}{Character. Language for the legend labels, either
\code{"en"} (default) or \code{"es"}.}

\item{na.translate}{Logical. Should \code{NA} values be displayed in the
legend? Default is \code{FALSE}.}
}
\value{
A \code{ggplot2} discrete scale object.
}
\description{
A \code{ggplot2} discrete fill scale that applies the official
MapBiomas Fuego Peru (Collection 1) color palette for a given sub-product
to a classified \code{SpatRaster} (as a factor), for use with
\code{tidyterra::geom_spatraster()}.
}
\examples{
\dontrun{
library(geoidep)
library(ggplot2)
library(tidyterra)

lima <- get_departaments("LIMA", show_progress = FALSE)

# Frecuencia de área quemada
freq <- get_mapbiomas_peru_fire(
  product = "frequency_burned",
  year = 2024,
  crop_to = lima
)

ggplot() +
  geom_spatraster(data = as.factor(freq)) +
  scale_fill_mapbiomas_peru_fire_d("frequency_burned", lang = "es") +
  theme_minimal() +
  labs(fill = "N° de quemas", title = "Frecuencia de área quemada 2013-2024")

# Año del último fuego
last_fire <- get_mapbiomas_peru_fire(
  product = "year_last_fire",
  year = 2024,
  crop_to = lima
)

ggplot() +
  geom_spatraster(data = as.factor(last_fire)) +
  scale_fill_mapbiomas_peru_fire_d("year_last_fire", lang = "es") +
  theme_minimal() +
  labs(fill = "Año", title = "Año del último fuego")
}
}
