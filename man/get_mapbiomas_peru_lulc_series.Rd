% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_mapbiomas_peru_lulc_series.R
\name{get_mapbiomas_peru_lulc_series}
\alias{get_mapbiomas_peru_lulc_series}
\title{Get a multi-year stack of MapBiomas Peru LULC rasters}
\usage{
get_mapbiomas_peru_lulc_series(years, crop_to = NULL, collection = 3)
}
\arguments{
\item{years}{Integer vector. Years to download (e.g. \code{2018:2024}).}

\item{crop_to}{Optional. An \code{sf}/\code{sfc} object, \code{SpatVector},
or \code{SpatExtent} defining the area of interest. If \code{NULL}
(default), each raster is returned at full extent.}

\item{collection}{Integer. MapBiomas Peru collection number. Default is \code{3}.}
}
\value{
A \code{SpatRaster} with one layer per year, named \code{classification_<year>}.
}
\description{
Downloads and stacks multiple single-year LULC rasters from MapBiomas
Peru (see \code{\link{get_mapbiomas_peru_lulc}}), each cropped to the same
area of interest if provided.
}
\examples{
\donttest{
library(geoidep)

lima <- get_departaments("LIMA")
lulc_series <- get_mapbiomas_peru_lulc_series(years = 2020:2024, crop_to = lima)
lulc_series
#> class       : SpatRaster
#> dimensions  : 4521, 5103, 5  (nrow, ncol, nlyr)
#> names       : classification_2020, classification_2021, ..., classification_2024
}
}
