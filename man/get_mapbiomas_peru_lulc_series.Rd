% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapbiomas-lulc.R
\name{get_mapbiomas_peru_lulc_series}
\alias{get_mapbiomas_peru_lulc_series}
\title{Get a multi-year stack of MapBiomas Peru LULC rasters}
\usage{
get_mapbiomas_peru_lulc_series(
  years,
  crop_to = NULL,
  collection = 4,
  show_progress = TRUE
)
}
\arguments{
\item{years}{Integer vector. Years to download (e.g. \code{2018:2024}).}

\item{crop_to}{Optional. An \code{sf}/\code{sfc} object, \code{SpatVector}, or \code{SpatExtent}. If \code{NULL}, each raster is returned at full extent.}

\item{collection}{Integer. MapBiomas Peru collection number (\code{1}, \code{2}, \code{3}, \code{4}). Default \code{3}.}

\item{show_progress}{Logical. Show a cli progress bar. Default \code{TRUE}.}
}
\value{
A \code{SpatRaster} with one layer per year, named \verb{classification_<year>}.
}
\description{
Downloads and stacks multiple single-year LULC rasters from MapBiomas
Peru (see \code{\link{get_mapbiomas_peru_lulc}}), each cropped to the same
area of interest if provided. Progress uses \link[cli:cli_progress_bar]{cli}.
}
\examples{
\dontrun{
library(geoidep)
lima <- get_departaments("LIMA", show_progress = FALSE)
lulc_series <- get_mapbiomas_peru_lulc_series(years = 2020:2024, crop_to = lima)
lulc_series
}
}
