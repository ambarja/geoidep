% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapbiomas-alerta.R
\name{get_mapbiomas_peru_alerta}
\alias{get_mapbiomas_peru_alerta}
\title{Download MapBiomas Alerta Peru deforestation alerts with spatial filtering}
\usage{
get_mapbiomas_peru_alerta(
  region = NULL,
  from = NULL,
  to = NULL,
  dsn = NULL,
  method = "within",
  show_progress = TRUE,
  quiet = TRUE,
  timeout = 60
)
}
\arguments{
\item{region}{An sf object specifying the area of interest (must be in EPSG:4326 - WGS 84).}

\item{from}{Character. Start date in \code{"YYYY-MM-DD"} format. If \code{NULL}, no lower bound.}

\item{to}{Character. End date in \code{"YYYY-MM-DD"} format. If \code{NULL}, no upper bound.}

\item{dsn}{Character. Output filename. If missing, a temporary GeoJSON file is created.}

\item{method}{Character. Spatial predicate used to filter the downloaded alerts against \code{region}:
\code{"intersects"} (safest default is \code{"within"} for backward compatibility), \code{"within"}, \code{"contains"}, \code{"crosses"}, \code{"touches"}.}

\item{show_progress}{Logical. Show cli progress. Default \code{TRUE}.}

\item{quiet}{Logical. Suppress messages from \code{sf::st_read()}. Default \code{TRUE}.}

\item{timeout}{Numeric. Seconds to wait for the server response (default 60).}
}
\value{
An sf object with alert geometries, attributes and image URLs.
}
\description{
Download the latest deforestation alerts detected by MapBiomas Alerta Peru
for a specific geographic area using an sf object (bounding box or polygon).
For more details, please visit: \href{https://alerta.mapbiomas.org/}{MapBiomas Alerta Platform}
}
\details{
The bounding box of \code{region} is used to query the WFS service. Image URLs
are generated from the alert \code{id} and point to public Google Cloud Storage buckets.
Column names are normalised to lowercase.
}
