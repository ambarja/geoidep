% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapbiomas-fire.R
\name{get_mapbiomas_peru_fire}
\alias{get_mapbiomas_peru_fire}
\title{Get a MapBiomas Peru Fire raster}
\usage{
get_mapbiomas_peru_fire(product, year, crop_to = NULL, collection = 1)
}
\arguments{
\item{product}{Character. One of the products listed in
\code{get_mapbiomas_peru_fire_products}, e.g. \code{"annual_burned"}.}

\item{year}{Integer. For \code{"annual"} products, the map year (from \code{1999}). For \code{"range"}
products (\verb{accumulated_*}, \code{frequency_burned}), the \strong{end year} (from \code{2014}, range starts 2013).}

\item{crop_to}{Optional. An \code{sf}/\code{sfc} object, \code{SpatVector}, or \code{SpatExtent}. If \code{NULL}, the full raster is returned.}

\item{collection}{Integer. MapBiomas Fuego Peru collection. Default \code{1} (only one available).}
}
\value{
A \code{SpatRaster} with one layer.
}
\description{
Lazily reads a MapBiomas Fuego (Fire) Peru raster from a given
sub-product, hosted as a GeoTIFF on Google Cloud Storage. Only the bytes
required for the requested extent are downloaded (via GDAL's
\verb{/vsicurl/} driver). Optionally crops and masks the raster to an
area of interest.
}
\examples{
\dontrun{
library(geoidep)
lima <- get_departaments("LIMA")
burned_2024 <- get_mapbiomas_peru_fire(product = "annual_burned", year = 2024, crop_to = lima)
}
}
