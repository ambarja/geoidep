% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapbiomas-lulc.R
\name{get_mapbiomas_peru_lulc}
\alias{get_mapbiomas_peru_lulc}
\title{Get MapBiomas Peru land use / land cover raster}
\usage{
get_mapbiomas_peru_lulc(year, crop_to = NULL, collection = 4)
}
\arguments{
\item{year}{Integer. Year of the classification (e.g. \code{2024}).}

\item{crop_to}{Optional. An \code{sf}/\code{sfc} object, \code{SpatVector}, or \code{SpatExtent}. If \code{NULL}, the full raster for Peru is returned.}

\item{collection}{Integer. MapBiomas Peru collection number (\code{1}, \code{2}, \code{3}, \code{4}). Default \code{3}.}
}
\value{
A \code{SpatRaster} with one layer named \verb{classification_<year>}.
}
\description{
Lazily reads a single-year Land Use and Land Cover (LULC) classification
raster from the MapBiomas Peru collection. Only the bytes required for the
requested extent are downloaded (via GDAL's \verb{/vsicurl/} driver).
Optionally crops and masks the raster to an area of interest.
}
\examples{
\dontrun{
library(geoidep)
lima <- get_departaments("LIMA", show_progress = FALSE)
lulc_2024 <- get_mapbiomas_peru_lulc(year = 2024, crop_to = lima)
lulc_2024
lulc_2025 <- get_mapbiomas_peru_lulc(year = 2025, collection = 4, crop_to = lima)
lulc_2025
}
}
