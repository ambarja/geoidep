% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igp.R
\name{get_igp_seismic_data}
\alias{get_igp_seismic_data}
\title{Download seismic data from the Geophysical Institute of Peru (IGP)}
\usage{
get_igp_seismic_data(
  catalog = c("instrumental", "historic"),
  start_date = NULL,
  end_date = NULL,
  min_magnitude = 4,
  max_magnitude = 10,
  min_depth = 0,
  max_depth = 900,
  polygon = NULL,
  dsn = NULL,
  show_progress = TRUE,
  timeout = 180
)
}
\arguments{
\item{catalog}{A string. One of \code{instrumental} (1960 to present) or
\code{historic} (1471-1959).}

\item{start_date, end_date}{Strings (\code{YYYY-MM-DD}). Only used when
\code{catalog = "instrumental"}; defaults to the last 30 days. Ignored when
\code{catalog = "historic"} (the repository fixes the period to
1471-01-01/1959-12-31).}

\item{min_magnitude, max_magnitude}{Numerics. Magnitude range filter.
Default \code{4} to \code{10}.}

\item{min_depth, max_depth}{Numerics. Depth range filter in km.
Default \code{0} to \code{900}.}

\item{polygon}{An \code{sf} object. Its bounding box is sent to the server to
narrow the download, and only the epicentres falling inside the polygon
are returned. Any CRS is accepted (reprojected internally to EPSG:4326).}

\item{dsn}{A string. Optional path where the raw XLSX file is saved.
Default \code{NULL} (temporary file).}

\item{show_progress}{Logical. Show cli progress. Default \code{TRUE}.}

\item{timeout}{Numeric. Seconds to wait for the server response. The IGP
service generates the XLSX on demand, so the default is \code{180}.}
}
\value{
An \code{sf} object (POINT, EPSG:4326). The \code{instrumental} catalog has
columns \code{fecha_utc}, \code{hora_utc}, \code{latitud}, \code{longitud},
\code{profundidad_km}, \code{magnitud}; the \code{historic} catalog reports three
magnitude scales (\code{magnitud_mb}, \code{magnitud_ms}, \code{magnitud_mw}).
}
\description{
Download the Peruvian seismic catalogs published by the Centro
Sismologico Nacional (CENSIS) of the Instituto Geofisico del Peru (IGP)
through the same endpoint used by the
\href{https://ultimosismo.igp.gob.pe/repositorio/datos-sismicos}{IGP seismic data repository}:
a \code{GET} request filtered by bounding box, magnitude and depth that returns
an XLSX file. The epicentres are returned as an \code{sf} POINT layer
(EPSG:4326) and, optionally, clipped to any user-supplied polygon with \code{sf}.
For more information, visit \href{https://www.igp.gob.pe/}{IGP}.
}
\details{
The XLSX response is parsed with base R only (\code{utils::unzip()} plus a small
internal reader), so no additional package is required.
Very wide queries (e.g. the full \code{historic} catalog with low magnitudes)
may exceed the server capacity (HTTP 502); narrow the filters and retry.
}
\examples{
\dontrun{
library(geoidep)
sismos <- get_igp_seismic_data(
  catalog = "instrumental",
  start_date = "2026-08-24",
  end_date = "2026-09-24",
  min_magnitude = 4,
  show_progress = FALSE)
head(sismos)
}
}
