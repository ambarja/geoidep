% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapbiomas-alerta.R
\name{get_mapbiomas_alert_images}
\alias{get_mapbiomas_alert_images}
\title{Download before/after deforestation images from MapBiomas Alerta Peru}
\usage{
get_mapbiomas_alert_images(
  alert_ids,
  download_dir = NULL,
  image_type = "both",
  show_progress = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{alert_ids}{Character or numeric vector. Alert IDs to download images for.}

\item{download_dir}{Character. Directory where images will be saved. If NULL, a temporary directory is used.}

\item{image_type}{Character. Which images to download: "both" (default), "before", or "after".}

\item{show_progress}{Logical. Show cli progress. Default \code{TRUE}.}

\item{overwrite}{Logical. Overwrite existing files. Default \code{FALSE}.}
}
\value{
A tibble with columns \code{alert_id}, \code{image_type}, \code{url}, \code{local_path}, \code{status}, \code{error_message}.
}
\description{
Download satellite imagery (before/after deforestation) for specific MapBiomas Alerta alerts
from public Google Cloud Storage buckets. Progress uses \link[cli:cli_progress_bar]{cli}.
}
\details{
File names follow the pattern \verb{alert_\{id\}_before_deforestation.png} and
\verb{alert_\{id\}_after_deforestation.png}. Each image has a 120-second timeout.
}
\examples{
\dontrun{
library(geoidep)
aoi <- get_departaments(show_progress = FALSE) |>
  subset(nombdep == "UCAYALI")
alerts <- get_mapbiomas_peru_alerta(region = aoi, show_progress = FALSE)
alert_ids <- alerts$id[1:5]
download_dir <- file.path(tempdir(), "mapbiomas_images")
results <- get_mapbiomas_alert_images(
  alert_ids = alert_ids,
  download_dir = download_dir,
  show_progress = FALSE
)
head(results)
}
}
