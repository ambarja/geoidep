% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_mapbiomas_alert_images.R
\name{get_mapbiomas_alert_images}
\alias{get_mapbiomas_alert_images}
\title{Download before/after deforestation images from MapBiomas Alerta Peru}
\usage{
get_mapbiomas_alert_images(
  alert_ids,
  download_dir = NULL,
  image_type = "both",
  show_progress = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{alert_ids}{Character vector or numeric vector. Alert IDs to download images for.}

\item{download_dir}{Character. Directory path where images will be saved.
If NULL, creates a temporary directory.}

\item{image_type}{Character. Which images to download: "both" (default), "before", or "after".}

\item{show_progress}{Logical. If TRUE, shows progress bar during download.}

\item{overwrite}{Logical. If TRUE, overwrites existing files (default FALSE).}
}
\value{
A data frame (tibble) with columns:
\itemize{
\item alert_id: The alert ID
\item image_type: "before" or "after"
\item url: Download URL
\item local_path: Path to downloaded file
\item status: "success" or "failed"
\item error_message: Error message if download failed
}
}
\description{
This function downloads satellite imagery (before/after deforestation) for specific MapBiomas Alerta alerts
from public Google Cloud Storage buckets.
}
\details{
Images are downloaded from public Google Cloud Storage URLs. File names follow the pattern:
\verb{alert_\{id\}_before_deforestation.png} and \verb{alert_\{id\}_after_deforestation.png}

The function uses HTTP GET requests with a 120-second timeout per image.
}
\examples{
\donttest{
library(geoidep)

# Download images for specific alerts
# First get some alerts
aoi <- get_departaments(show_progress = FALSE) |>
  subset(nombdep == "UCAYALI")

alerts <- get_mapbiomas_peru_alerta(region = aoi, show_progress = FALSE)

# Get alert IDs
alert_ids <- alerts$id[1:5]  # First 5 alerts

# Get images to a directory
download_dir <- file.path(tempdir(), "mapbiomas_images")
results <- get_mapbiomas_alert_images(
  alert_ids = alert_ids,
  download_dir = download_dir,
  show_progress = TRUE
)

head(results)
}

}
